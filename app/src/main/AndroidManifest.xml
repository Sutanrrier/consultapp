<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apppreconsulta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFirebase">

        <activity android:name="com.example.apppreconsulta.MarcarConsulta"
            android:exported="true" />

        <activity android:name="com.example.apppreconsulta.Consulta"
            android:exported="true" />

        <activity android:name="com.example.apppreconsulta.TelaFormulario"
            android:parentActivityName="com.example.apppreconsulta.MainActivity"/>

        <activity android:name="com.example.apppreconsulta.TelaEntrar" />

        <activity android:name="com.example.apppreconsulta.MainActivity"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>